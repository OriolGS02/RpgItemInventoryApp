<Window x:Class="MyConsoleApp.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyConsoleApp.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Text="Inventory"
            Grid.Row="0"
            FontSize="24"
            HorizontalAlignment="Center"
            Margin="10"/>
        <ListBox 
            x:Name="inventoryList"
            Grid.Row="1"
            Margin="10"
            SelectionChanged="InventoryList_SelectionChanges">
            
            <ListBox.ItemTemplate>
                <DataTemplate>

                    <StackPanel Margin="5">

                        <TextBlock
                            Text="{Binding Name}"
                            FontSize="18"
                            FontWeight="Bold"/>

                        <TextBlock
                            Text="{Binding Type,StringFormat=Type: {0}}"/>

                        <TextBlock
                            Text="{Binding Quantity,StringFormat=Quantity: {0}}"/>

                        <TextBlock
                            Text="{Binding Description,StringFormat=Description: {0}}"/>

                    </StackPanel>

                </DataTemplate>
            </ListBox.ItemTemplate>
            
           </ListBox>




    </Grid>
</Window>
